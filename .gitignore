# Ignore everything by default
/*

# Allow directories (so we can include specific files inside them)
!*/

# Keep specific files
!.gitignore
!run.sh
!*.ipynb
!SL_SS3xpress_env.yaml

# Ignore swap and checkpoint files
.ipynb_checkpoints/
*/*.swp

# Include everything in the bin folder
!bin/**
# Ignore bin checkpoints and pycache
bin/.ipynb_checkpoints/
bin/__pycache__/

# Ignore everything in input by default
input/**

# Un-ignore the input folder itself
!/input/

# Include only specific files in input folder
!/input/N*indexes.fasta
!/input/TAGs.fasta
!/input/genome_and_annotation/
!/input/genome_and_annotation/*

# Ignore everything in output/
output/**

